

val m1 = List(2, 3, 5, 7, 8)
          
val result = m1.mkString("s","/","e")



/* def meshToXml() =
    (0 until size2).map().mkString("<row" + _ + ">", "", "</row" + _ + ">")
    (0 until size).map().mkString("<" + _ + ">", " ", "</" + _ + ">")
    matrix.row(0 until size2)
*/

(0 until 7).map(_ => "x").mkString("<row" + 2 + ">", "6", "</row" + 2 + ">")
(0 until 7).map(_ => "x").map("row"+_ ) //mkString("<row" + 2 + ">", "6", "</row" + 2 + ">")
//(0 until 6).map("x").mkString("<" + 2 + ">", " ", "</" + 2 + ">")
val 
beschissenerString : String = ""
beschissenerString.appendedAll("fuck")
val xs = Seq(1, 2, 3)
//for (i <- xs) {print(beschissenerString = beschissenerString.append + i.toString)}
beschissenerString